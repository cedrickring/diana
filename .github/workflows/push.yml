on: push
name: Push
jobs:
  checks:
    name: checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: Run all checks
        uses: cedrickring/golang-action@v2.0.0-beta
